# EXPERIMENTS.md — Master Tracking Table

One row per directory in `experiments/` (54 total, plus 13 `desk_*` experiments in section 6). This is a top-level index, not a
replacement for the READMEs — every row links back to `experiments/<name>/README.md`,
which has the full methodology, tables, and caveats. Numbers here are the headline
figures each README itself treats as the bottom line.

**Shared frame.** Almost every experiment below is scored against the same reference
points, established early and reused verbatim:

- **The composite** (`experiments/largecap/README.md`): a no-fit, equal-weight blend of
  7 factor groups (18 factors) on a ≥$2B-market-cap, ≥$10M-dollar-volume, both-betas-observed
  universe. Universe IC 0.037 (t 1.9); its `lc_t10` long-short LP: gross IR 0.615, net IR
  0.541, 2025 net return −12.4%.
- **The pre-registered kill/pass rule** used by most `feat_*` and methodology experiments:
  KILL if the paired IC gain over the composite has t < 1 or the gain is confined to the
  smallest size tercile; PASS requires paired t ≥ 2 **and** Bonferroni-adjusted permutation
  p ≤ 0.05 **and** not small-tercile-only. Most experiments land in between (t between 1
  and 2) and are recorded as inconclusive, not killed or passed.
- **`pm_ablation`**: the diagnostic finding that every model's real-universe edge
  (rank IC ≈0.14–0.15) collapses to ≈0 once FIAM's tradeability screens (price ≥ $5,
  mcap ≥ $500M, sector caps, turnover cap) are applied — the edge lives in sub-$5,
  sub-$500M, mostly biotech short names. This is the project's central negative result
  and is why almost every model experiment below reports the same shape: strong on the
  full universe, flat-to-negative once constrained to a tradeable book.

**Reading the columns.** *Verdict* is the practical takeaway (would this be shipped/reused
today), not just the README's literal status token. *Pursue further?* is this table's own
judgment call, not a project decision — treat it as a prioritization hint.

**Where this nets out.** Two things cleared the bar outright: `feat_short_interest`'s
days-to-cover block passed the pre-registered rule on IC, and its `feat_si_followup`
turned that into an adopted short-book risk constraint (SI-based short cap). `poly.py` is
the best risk-adjusted single model (IR 0.87, Sharpe 1.08) and is fully attributable
after its own leakage fix. `ols.py` and `largecap`'s composite are the two standing
baselines everything else is measured against. A handful of experiments
(`composite_overlap`, `truncation_audit`, `expectation_calibration`, `pm_ablation`,
`pca_diagnostic`, `borrow_stress`) are diagnostics with no adopt/reject rule — their value
is the standing methodology or caveat they established, not a signal. Almost everything
else (~40 of 54) failed or was inconclusive against the pre-registered rule, which is
itself the project's dominant finding: very little survives contact with the tradeable
universe.

---

## 1. Foundational baselines & structural diagnostics

These set the frame (the composite, the LP, the kill rule, the leakage bar) that every
other experiment below is measured against.

| Experiment                                                                    | Objective                                                                                                                                                              | Key result                                                                                                                                                                                                                                            | Verdict                                                           | Pursue further?                                                  | Notes                                                                                                                                                         |
| ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`ols`](../experiments/ols/README.md)                                         | Establish the competition-required linear floor (OLS on 147 chars, beta-neutral LP) before anything fancier                                                            | OOS R² −0.0086% (pooled); IR 0.85; Sharpe 0.98; CAGR 27.6%; alpha t 2.41; max DD −42.4%                                                                                                                                                               | **Adopted** — the standing baseline/floor                         | N/A — reference point                                            | Defines the "legacy" LP (screens, beta-neutral, 1% cap, 200% gross) reused by nearly every model script; R²≈0 alongside strong returns is a recurring caution |
| [`largecap`](../experiments/largecap/README.md)                               | Can a large-cap-only universe rescue tree-model edge, or does a simple factor composite work there instead                                                             | ET: IC 0.016, gross IR negative every seed. Composite (no fitting): universe IC 0.037 (t 1.9); `lc_t10` gross IR 0.615/net 0.541; 2025 net −12.4%                                                                                                     | **Adopted** (composite) / killed (ET on large caps)               | Maybe — composite is the reference signal reused project-wide    | **Foundational**: the frozen composite and `lc_t10` LP defined here are reused verbatim as the baseline in ~10 other experiments                              |
| [`pm_ablation`](../experiments/pm_ablation/README.md)                         | Isolate which FIAM constraint (price/mcap screens, dual-beta, sector limits, turnover cap) turns strong "legacy" IR into ~zero on a real book                          | Legacy IR 0.90–1.26 gross across 5 models → PM book (`pm_free`/`pm_t10`) −0.24 to +0.07 gross; tradeable-universe IC only 0.034–0.037 (vs 0.14–0.15 all-stocks)                                                                                       | Diagnostic only (killed the "model edge is tradeable" hypothesis) | N/A — pivotal, spawned `largecap` and `macro_condition`          | **Structural**: shows the whole tree-model edge lives in sub-$5/sub-$500M biotech/meme shorts; reframes the project's honest-negative-result narrative        |
| [`composite_overlap`](../experiments/composite_overlap/README.md)             | Is the composite mostly the commoditized "shared core" risk premium? Shapley decomposition, 3 IC definitions, screen comparison                                        | Raw IC 0.0366 (t 1.91); risk-model-residual IC 0.0252 (t 2.67, survives orthogonalization); vs a plain be_me+qmj screen IC 0.0311 (paired diff +0.0055, t 0.42 — indistinguishable); Shapley: value = 54% of IC                                       | Diagnostic only                                                   | Yes — sets the "judge every candidate by residual IC" standard   | Used as the methodology standard by every `feat_*` experiment since; composite ≈ a value+quality screen, weak where actually tradeable (large tercile)        |
| [`truncation_audit`](../experiments/truncation_audit/README.md)               | Verify no return-derived characteristic leaks future info and the label is exactly next-month return                                                                   | Truncation test passes for all 7 return chars (max diff ≤1e-13); label alignment 100% exact (522,431 rows); 11 chars with \|IC\|>0.10 all-stock but max 0.034 in tradeable universe (none flagged)                                                    | Diagnostic only — audit passed                                    | Yes — adopted as standing practice for every new feature builder | High-IC chars are the known small-cap lottery/ivol family, not leakage                                                                                        |
| [`expectation_calibration`](../experiments/expectation_calibration/README.md) | How should the composite's headline numbers be quoted, given production haircuts, fundamental-law realization, and statistical power                                   | Unconstrained law implies IR 1.86 vs realized gross 0.615 (transfer coefficient 0.33); 40–57% haircut → net IR 0.02–0.18; min detectable IC at 80% power = 0.054 (composite needs 143 months, has 68); bootstrap 5/50/95 net IR = [−0.17, 0.54, 1.22] | Diagnostic only — no adopt/kill rule                              | Yes — used to caveat every headline number since                 | IC decayed within-sample (2021–23: 0.054 vs 2024–26: 0.018, not significant) — consistent with the composite being the crowded core                           |
| [`pca_diagnostic`](../experiments/pca_diagnostic/README.md)                   | Are the panel's top-15 principal components stable enough to support a future PCA/shrinkage-prior model (prerequisite check, not a strategy)                           | Split-half subspace stability (cosine) > 0.95 for k=1–15 except k=6 (0.861, eigenvalue-tie artifact); 8 PCs needed for 50% variance, >15 for 90%                                                                                                      | Diagnostic only — no model was built                              | Maybe — "green light with a dimensionality caveat" if pursued    | Prerequisite for an unbuilt Kozak–Nagel–Santosh-style shrinkage model; only 6 walk-forward windows tested                                                     |
| [`univariate`](../experiments/univariate/README.md)                           | Standalone IC and single-factor OOS R² for each of the 147 characteristics, individually, no interactions (no README — description from source docstring + output CSV) | Top single-factor ICs ≈0.056–0.062 (`ebitda_mev` +0.062 t4.18, `ocf_me` +0.061 t4.20, `eqnpo_12m` +0.059 t4.40, `chcsho_12m` −0.058 t−4.60); every single-factor OOS R² is ≈0 (all within ±0.0001)                                                    | Diagnostic only (reference table)                                 | N/A — reference lookup, not a candidate                          | **README missing** — only `univariate.py` + `output/` exist; treat feature-level numbers as reference data for other `feat_*` writeups, not a tested strategy |

## 2. Models

Alternative estimators/architectures on the same (or a closely related) panel and LP harness.

| Experiment                                    | Objective                                                                                                                                      | Key result                                                                                                                                                                                      | Verdict                                                          | Pursue further?                                                                   | Notes                                                                                                                                                                                |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [`rf`](../experiments/rf/README.md)           | Replicate a "Graham vs. Modern factors vs. Combined" horse race (paper predicts Graham value-rules win) inside the project's beta-neutral LP   | Modern IR 1.05, Sharpe 1.21, alpha t 2.84, CAGR 30%; Graham IR −0.23 (a losing strategy); Combined IR 0.81                                                                                      | Not adopted as literal replication; Modern flagged for follow-up | Yes                                                                               | First of the RF trilogy → `rf_2` → `rf_3`; its own short-squeeze explanation for Graham's failure is later disproven in `rf_2`                                                       |
| [`rf_2`](../experiments/rf_2/README.md)       | Stress-test `rf.py`'s Modern-arm IR 1.05: is the 15-factor set special, is tuning real, does the squeeze explanation hold                      | Honest IR ≈1.0–1.1 gross (seed range 1.06–1.20), ~0.9 net at 10bp; 15-random-factor null mean IR 1.02 (Modern is *not* special); Graham IC 0.005, below every random draw                       | Diagnostic only — narrows the claim, keeps Modern as a candidate | Yes                                                                               | Second of the trilogy; overturns `rf.py`'s squeeze story; break-even cost ≈105bp; short book concentrated in sub-$2B/meme names                                                      |
| [`rf_3`](../experiments/rf_3/README.md)       | Does the Modern-RF edge survive momentum removal and a realistic PM-constrained (price≥$5, mcap≥$500M, sector/turnover-capped) LP              | Legacy LP IR 1.26 (momentum-free) / 1.33 (with momentum); PM LP (`pm_t10`) IR ≈0 gross / −0.31 net; test rank IC 0.14 all-stock vs ~0.035 tradeable                                             | Not adopted for the PM book; legacy-LP number kept as reference  | No, on this construction                                                          | Third of the trilogy; alpha lives in the short leg / sub-$500M / biotech names per `pm_ablation`                                                                                     |
| [`xgb`](../experiments/xgb/README.md)         | Does validation-tuned XGBoost on 147 characteristics capture nonlinear structure OLS misses                                                    | OOS R² −0.127% (worse than OLS −0.009%); IR 0.12 vs OLS 0.85; alpha t 0.96 vs OLS t 2.41; 3 of 6 calendar years negative                                                                        | Not adopted (worse than the OLS floor)                           | Yes — root cause fixed in `xgb_2`                                                 | First of the XGB pair; the defect was validation-MSE-based tuning (2025 fold early-stopped after 4 trees), not "trees don't work"                                                    |
| [`xgb_2`](../experiments/xgb_2/README.md)     | Fix `xgb.py`'s tuning (select on validation rank IC not MSE, forest-style regularization, winsorized label) — does XGBoost recover             | Legacy LP IR jumps 0.12→1.25 (139 feat) / 1.04 (15 feat) / 0.90 (12 feat); OOS R² near zero; PM-constrained book still fails (IR −0.29 to −0.54 gross)                                          | Adopted for legacy-LP benchmarking; not adopted for the PM book  | Yes — confirms model choice isn't the constraint                                  | The failure in `xgb.py` was tuning, not the model family; PM-LP failure mirrors `rf_3` across model families                                                                         |
| [`et`](../experiments/et/README.md)           | Extra-Trees (max randomization, no bootstrap) as a lower-variance alternative to RF for a near-noise label                                     | Test IC 0.1516 (modern_nomom); legacy IR 0.96/0.81 gross/net (0.97/1.13 for `modern`); `pm_t10` IR −0.28/−0.37; OOS R² +0.154%                                                                  | Not adopted (inconclusive)                                       | No — same tradeable-universe failure as the batch                                 | Feature importance concentrated in the lottery/vol pair (`rmax5_21d`, `ivol_capm_21d` = 44% combined)                                                                                |
| [`cat`](../experiments/cat/README.md)         | CatBoost (oblivious trees, ordered boosting, heavy regularization) as another tree-model arm                                                   | Test IC 0.1442/0.1445; legacy IR 0.92/0.78 and 1.19/1.03 gross/net; `pm_t10` IR −0.31/−0.41; OOS R² +0.176%/+0.173%                                                                             | Not adopted (inconclusive)                                       | No — same ceiling/failure mode as the RF/ET/LGBM/XGB batch                        | `betabab_1260d` carries 30% of feature importance (vs 11–12% elsewhere)                                                                                                              |
| [`lgbm`](../experiments/lgbm/README.md)       | Does forest-style LightGBM beat Random Forest / other tree models on IC and the legacy portfolio                                               | Universe rank IC 0.1545 (nominally highest of the batch) but legacy IR gross 0.92/net 0.80, below RF's 1.26/1.09; validation picked the smallest tree count (50) in 5/6 folds                   | Not adopted                                                      | No — no gain over RF, same PM-tradeability ceiling                                | One of 5 models feeding `pm_ablation`'s analysis; same untradeable-edge conclusion as every other tree model                                                                         |
| [`poly`](../experiments/poly/README.md)       | Interpretable degree-2 polynomial (top-20-by-IC features) as a tradeable model, plus a surrogate reproducing XGBoost's predictions             | Post-leakage-fix: IR 0.87, Sharpe 1.08 (best of its 8-model batch), CAGR 21.2%, alpha t 2.74, max DD −21.4%; pre-fix (leaky) IR was 1.46                                                        | **Adopted** — best risk-adjusted model in its batch              | Yes — best Sharpe/IR in batch, fully attributable                                 | Caught and fixed its own look-ahead bug (feature selection reading an OOS-window IC file); fix dropped IR 1.46→0.87 — a leakage case study kept as a project reference               |
| [`analog`](../experiments/analog/README.md)   | K-NN "lookalike" memory-based forecasting vs. fitted trees; does restricting memory to tradeable stocks preserve alpha                         | IC 0.1395 all-stock, legacy IR 1.04/0.85; tradeable IC 0.032, `pm_t10` IR −0.48/−0.58; K=1600 (grid edge) chosen in all 6 folds                                                                 | Diagnostic only / not adopted                                    | No — same ceiling and same tradeable-universe failure as trees                    | Rank-correlates 0.71–0.86 with RF/ET/XGB predictions — not a distinct signal, despite the explainability angle                                                                       |
| [`rankglu`](../experiments/rankglu/README.md) | Reproduce RankGLU (gated linear+nonlinear score-formation net), validated in its source paper only on Chinese equities                         | Widened-training version: IR 0.57, Sharpe 0.77, CAGR 14.0%, realized beta −0.019 (t −0.12, fixed); undertrained first version had beta −0.47 (t −2.74) — an outright neutrality-mandate failure | Not adopted                                                      | Maybe — architecture works after fixing, but still weakest IR/Sharpe of its batch | Neutrality bug traced to undertraining (8 epochs), not architecture; fixed via 20 epochs — a cautionary case for judging architecture only after adequate training                   |
| [`e2e`](../experiments/e2e/README.md)         | End-to-end portfolio-weight learning via SPSA on the paper's composite reward (Sharpe − turnover penalty − benchmark corr), vs. OLS warm start | 5/6 folds improve over the OLS warm-start objective; OOS R² +0.068% (one of only 2 positive-R² models, with `poly.py`); IR 0.79 vs OLS 0.85; turnover 38.2% vs 39.7%                            | Not adopted (inconclusive)                                       | Maybe — one of two positive-R² models, worth a lambda sweep                       | Large fidelity gap to the source paper (SPSA on linear theta, no RL/CNN-GRU/PPO)                                                                                                     |
| [`joint`](../experiments/joint/README.md)     | Portfolio-aware weighted OLS (weight = \|rank\|^p, p chosen on validation-period portfolio Sharpe) vs. plain OLS, per "ML Meets Markowitz"     | IR 0.75 vs OLS 0.85; Sharpe 0.88 vs 0.98; CAGR 23.3% vs 27.6%; selected p unstable across folds (0,1,2,0,0,4)                                                                                   | Not adopted                                                      | No — validation criterion appears to chase per-fold noise                         | Fixed a real validation bug (uniform-MSE selection always degenerated to p=0) before testing; no edge over OLS even after the fix                                                    |
| [`sparse`](../experiments/sparse/README.md)   | Random-Fourier-feature expansion + LASSO sparse recovery vs. ridgeless dense fit ("Virtue of Sparsity")                                        | LASSO: 0 nonzero coefficients at every fold, every alpha (1e-7–3e-2); Sparse IR −0.47 (near-flat) vs Ridgeless IR −0.70 (active noise-fitting, CAGR −2.16%)                                     | Not adopted                                                      | No                                                                                | Fixed a real bug (RFF scale collapsed LASSO to zero pre-standardization); P=600 doesn't reach the paper's P>n regime at this project's sample sizes — "sparse wins by doing nothing" |

## 3. Feature / candidate-signal tests

Each adds one or more blocks to the composite (or a model's inputs) and is scored on
paired IC gain against it, per the shared kill/pass rule above.

| Experiment                                                              | Objective                                                                                                                                            | Key result                                                                                                                                                                                                                                         | Verdict                                                                           | Pursue further?                                                                 | Notes                                                                                                                                                                       |
| ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`btq`](../experiments/btq/README.md)                                   | Factor-level Beta-Times-Quantity interaction terms (7 named factors × trading intensity)                                                             | Primary OOS R² −0.00237% vs ablation −0.00545% (beats ablation); portfolio IR 0.67 vs OLS baseline 0.85; both R² ≈0                                                                                                                                | Not adopted (inconclusive)                                                        | No — effect indistinguishable from zero, weaker portfolio result than plain OLS | Named-factor set is this project's own choice, not from the source paper                                                                                                    |
| [`factor_filter`](../experiments/factor_filter/README.md)               | Does filtering/preprocessing the 147-factor panel (decorrelation, IC-based selection) raise OOS or tradeable performance                             | Decorr pruning: no gain (paired t −0.5); `ic_top30` investable IC 0.034 (t 2.3) vs all-139 0.023; best `pm_t10` gross IR still −0.21                                                                                                               | Not adopted for performance; adopted for hygiene (dedup)                          | Maybe — dedup + a pre-specified group-based factor set                          | Train-era IC does not predict test-era IC in the investable universe (rank corr 0.10) vs all-stocks (0.89) — persistent edge lives in the untradeable small-cap/vol cluster |
| [`feat_8k_meta`](../experiments/feat_8k_meta/README.md)                 | Test 10 structured 8-K metadata blocks (filing frequency, decayed bad-event/mgmt-change intensity, no-news reversal, earnings-timing flags)          | All 10 blocks fail; paired gains −0.0038 to +0.0010 (t −1.68 to +0.47); one standalone lead `mgmt_5_02_decayed_hl6` IC +0.0125 (t 2.87, perm p 0.010) but paired gain only +0.0010                                                                 | Killed                                                                            | No — one weak lead spun into its own followup                                   | Falsifies 3 literature claims in this universe; the 5.02 flag spawned `feat_8k_502_followup`                                                                                |
| [`feat_8k_502_followup`](../experiments/feat_8k_502_followup/README.md) | Is the 8-K item 5.02 (officer/director change) intensity signal real, or a size/liquidity proxy / multiple-testing artifact                          | Standalone IC +0.0101 to +0.0133 (t 2.4–3.0), survives size/turnover/Amihud/age orthogonalization; paired composite gain only +0.0010 to +0.0013 (t 0.5–0.6); 4x-weight gain −0.0051                                                               | Not adopted (inconclusive)                                                        | No — effect too small to matter                                                 | Genuine but tiny (IC ≈0.012, a third of the composite's) — dilutes rather than helps at any blend weight; Bonferroni p 0.05–0.10, borderline                                |
| [`feat_blitz`](../experiments/feat_blitz/README.md)                     | Test a Blitz et al. large-cap short-term composite (industry-relative reversal, industry momentum, seasonality, ivol) as an 8th composite group      | All 5 blocks/composites killed; paired gains −0.0052 to +0.0004; industry-relative reversal wrong-signed standalone (IC −0.0105, t −1.03)                                                                                                          | Killed (analyst-revision leg BLOCKED — no I/B/E/S data)                           | Maybe — only if I/B/E/S revisions become available                              | Tested a weaker 3-signal composite since the revisions leg is blocked                                                                                                       |
| [`feat_char_changes`](../experiments/feat_char_changes/README.md)       | Do 3/12-month changes in cross-sectional rank of profitability/valuation/quality/surprise/investment ("momentum in attributes") add IC beyond levels | All 7 blocks killed; 12m composite gain −0.0050 (t −1.47); individual changes gain −0.0052 to −0.0016                                                                                                                                              | Killed                                                                            | No                                                                              | Documents a smoke-test trap: one variant looked good (+0.008) on a 1/3 sample, was −0.0040 on the full sample                                                               |
| [`feat_industry_rel`](../experiments/feat_industry_rel/README.md)       | Test industry-adjusted momentum variants and within-industry-ranked replacement composites (GICS/FF49/blends)                                        | All blocks/composites fail; momentum-variant gains −0.0007 to +0.0030; within-industry composites worse (IC 0.0324 vs 0.0366)                                                                                                                      | Killed                                                                            | No — idea exhausted with this panel                                             | Value/investment tilts are partly *between*-industry effects that within-industry ranking removes, hurting rather than helping                                              |
| [`feat_insider_form4`](../experiments/feat_insider_form4/README.md)     | Test SEC Form 4 insider open-market purchase/sale value and cluster-buying blocks                                                                    | All 4 blocks killed; paired gains ≤+0.0017; best standalone `ins_sell_6m` IC +0.0188 (t 1.55)                                                                                                                                                      | Killed                                                                            | Maybe — role-weighted refinement untried                                        | Insider purchases are rare in $2B+ names (mostly scheduled selling); the literature's real alpha source (role/opportunistic-only split) not attempted                       |
| [`feat_path`](../experiments/feat_path/README.md)                       | Test price-path-shape features (information discreteness, trend/slope-R², up/down fraction, drawdown), incl. frog-in-the-pan momentum×continuity     | All 5 blocks killed; paired gains −0.0016 to +0.0011; mom×continuity IC ≈ plain momentum, no interaction visible                                                                                                                                   | Killed                                                                            | Maybe — daily-return version untried (data not currently available)             | Monthly returns give only an 11-sign path, a coarse proxy vs. the literature's ~250-sign daily measure                                                                      |
| [`feat_peer_gap`](../experiments/feat_peer_gap/README.md)               | Test correlation-based peer (top-5 by 60m return correlation within FF49) return/gap/momentum signals vs. a plain industry-return control            | All 4 blocks killed; gains −0.0042 to +0.0005; peer signal no better than the industry control (IC 0.0079 vs 0.0003)                                                                                                                               | Killed                                                                            | No — superseded by the TNIC version                                             | Lead-lag not visible at monthly frequency in large caps; replaced by `feat_tnic_peers` (text-based peers), which also failed                                                |
| [`feat_tnic_peers`](../experiments/feat_tnic_peers/README.md)           | Test Hoberg–Phillips TNIC-3 text-similarity peer (top-5) return/gap/momentum signals vs. an industry-return control                                  | All 4 blocks killed; `peer_gap` wrong-signed and significant-negative (IC −0.0113 t −1.80, paired gain −0.0046 t −2.07) — opposite of the "laggards catch up" hypothesis                                                                           | Killed                                                                            | No — peer-return idea now exhausted at monthly frequency                        | Network staleness (77.5% coverage, 2–3yr stale by 2025–26) muddies interpretation                                                                                           |
| [`feat_short_interest`](../experiments/feat_short_interest/README.md)   | Test FINRA short-interest ratio, 3m change, and days-to-cover as composite additions and a short-book risk filter                                    | `days_to_cover` **passes** the pre-registered rule: paired gain +0.0089 IC (t 2.67), resid IC +0.0334 (t 3.60), perm p 0.005; `si_ratio` close miss; `si_change_3m` killed                                                                         | **Adopted** (conditionally) for days-to-cover; not adopted for si_ratio/si_change | Yes — triggered `feat_si_followup`                                              | Only block in its batch to pass the pre-registered rule outright; Bonferroni over the project-wide ~46-block family turns p 0.005 into ≈0.23                                |
| [`feat_si_followup`](../experiments/feat_si_followup/README.md)         | Scrutinize the days-to-cover/SI signal: leg attribution, staleness, borrow-cost stress, and use as a short-side risk filter                          | ~50% of the decile spread is long-side (low-SI) outperformance, not short-side; IC unchanged on a 6-month-stale file (persistent state, not a timing signal); SI≤10% short cap cuts max DD −18.9%→−9.8%, 2025 loss −12.4%→−6.6%, net IR ~unchanged | **Adopted** as a portfolio risk constraint, not as an alpha block                 | Yes — SI-based short cap recommended                                            | Most defensible use is squeeze/borrow risk filtering on the short book; the alpha gain doesn't translate to a significant LP IR gain                                        |
| [`feat_size_interact`](../experiments/feat_size_interact/README.md)     | Test size-tercile-conditional composites: ranking within size tercile, or IC-weighting factor groups per tercile (fitted on prior data)              | Both killed; within-tercile ranking ~no change (IC 0.0350 vs 0.0366); IC-weighted version clearly worse (IC 0.0241, gross IR 0.25 vs 0.61)                                                                                                         | Killed                                                                            | No                                                                              | IC-weighted variant fails for the same non-persistence reason found in `factor_filter` — group-IC weights fit on 2015–2020 don't carry to 2021–26                           |
| [`macro_condition`](../experiments/macro_condition/README.md)           | Does adding yield-curve slope / credit spread / VIX to tree inputs raise universe IC over a placebo/noise floor                                      | ET `plus_ycurve` IC 0.0345 (t 2.50) vs control 0.0164, paired t only 1.19 — below the ≥2 rule; placebo noise floor ≈±0.006 IC; trees put 45–90% importance on macro columns in every arm, including placebos                                       | Not adopted (inconclusive)                                                        | Maybe — yield-curve slope mildly suggestive, not credible yet                   | Gain concentrated in a few months (2022 curve inversion); portfolio IR does not follow IC (`lc_t10` gross IR −0.18 to −0.36)                                                |

## 4. Portfolio construction & risk management

Sizing, constraints, and stress-testing on top of an existing signal (usually the composite) — not new alpha.

| Experiment                                                                    | Objective                                                                                                                                                               | Key result                                                                                                                                                                                                                    | Verdict                                                                     | Pursue further?                                                                              | Notes                                                                                                                                                                             |
| ----------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`borrow_stress`](../experiments/borrow_stress/README.md)                     | Does the composite book's net IR (0.541) survive 2–20x borrow multipliers and forced-cover shocks                                                                       | Net IR 0.541→0.499 (x2)→0.373 (x5)→0.163 (x10)→−0.256 (x20); forced cover at +15%/mo: mean IR −0.005, max DD −31%; at +30%: IR −0.55, DD −44%                                                                                 | Diagnostic only                                                             | Maybe — motivates the short-interest cap already built                                       | Book tolerates uniform borrow cost up to ~10x; the real fragility is persistent forced-cover/squeeze state — directly motivated `feat_si_followup`                                |
| [`conformal_select`](../experiments/conformal_select/README.md)               | Conformal p-values + BH FDR control on the composite score, for calibrated long/short selection vs. same-size top-k                                                     | Long-side realized FDR 0.69–0.72 exceeds target q; short-side FDR 0.55–0.57 controlled; selection is mechanically identical to top-k (no ranking gain)                                                                        | Killed                                                                      | No — mechanically identical to top-k, exchangeability breaks on the long side                | Confirms the composite's edge is in avoiding/shorting the worst names, not picking winners — matches `portfolio_concentration`                                                    |
| [`crowding_gate`](../experiments/crowding_gate/README.md)                     | Do crowding-state gates (trailing IC, sleeve moves, book momentum, S&P vol) or vol scaling cut drawdown/unwind losses cheaply                                           | Best gate (S&P vol) +0.015 IR but fires 4% of months and misses every unwind; trailing-IC gate −0.129 IR and also misses unwinds; vol-scaling −0.159 IR, DD −21.8%                                                            | Killed                                                                      | No — needs real crowding data (e.g. 13F overlap), not available                              | Confirms IC-based gates miss crowding unwinds (a variance shift, not IC decay); a validation-tuned cutoff failed to transfer as predicted                                         |
| [`hrp`](../experiments/hrp/README.md)                                         | Does Hierarchical Risk Parity sizing on top of the existing composite book lower vol/drawdown vs. equal weight, without hurting neutrality (sizing only, no new signal) | At $2B/2% cap: HRP Sharpe net 0.88 vs ew 0.84, max DD −13.7% vs −19.0% (better) but beta t −1.08 vs −0.53 (worse neutrality) — fails the pre-registered neutrality leg; fails outright at a $1B floor                         | Not adopted (fails the pre-registered rule)                                 | No — inverse-vol (IVP) sizing is the cheaper equivalent                                      | HRP's drawdown benefit is not incremental over simpler IVP sizing (similar/lower vol at ~half the turnover)                                                                       |
| [`neutral_dial`](../experiments/neutral_dial/README.md)                       | Does deeper neutralization (size/momentum/vol/quality) reduce stress-window losses, and at what IR cost                                                                 | Full neutral (D4) cuts the 2025 loss −12.4%→−8.4% and roughly halves unwind-month losses, at net IR 0.43 vs baseline 0.54 (paired t −0.6, not significant); size/momentum-only neutralization loses IR with no stress benefit | Not adopted (inconclusive)                                                  | Maybe — flagged as a defensible risk-oriented variant                                        | Only 4 stress months + 1 squeeze; reconciles conflicting literature — deep neutrality helps stress P&L only when it hits the shared "core" (vol/quality), not size/momentum alone |
| [`portfolio_buffer`](../experiments/portfolio_buffer/README.md)               | Does spending the turnover budget via rank buffer / L1 penalty / partial rebalance / EMA-smoothed signal beat the hard 10% turnover cap                                 | Every variant has lower net IR than its matching control (e.g. 0.469 vs 0.541); best L1 variant +0.07 IR (t 0.28, noise); no variant meets the rule (net IR ≥ control+0.10, t≥1)                                              | Killed (buffer/L1/partial/EMA family)                                       | No — the hard cap already beats the alternatives here                                        | The composite's slow signal (rank autocorr 0.92) means a free LP just trades noise; a concentration variant (higher cap) did pass separately — see `portfolio_concentration`      |
| [`portfolio_concentration`](../experiments/portfolio_concentration/README.md) | Does raising the per-name cap (0.5–3%) concentrate the book into highest-conviction names and raise IR; do 30-bucket returns show extremes carry the alpha              | 2% cap: net IR 0.724 vs 1% control 0.541 (paired t 1.85) but max DD −32% vs −19%, 2025 return −21.6% vs −12.4%; 30-bucket spread not monotone — alpha is mostly avoiding the worst ~7%, not the top extremes                  | Not adopted ("modest, unconfirmed IR gain with materially higher drawdown") | Maybe — worth retesting with a risk-based (not IR) criterion                                 | Rule technically met at 1.5%/2% caps, but 8 variants tested (Bonferroni p≈0.3); 3% cap violates FIAM's ≥100-position rule                                                         |
| [`tpa`](../experiments/tpa/README.md)                                         | Allocate capital across 7 factor-group "sleeves" by equal-risk-contribution (ERC) or shrunk max-Sharpe vs. equal-capital composite                                      | ERC beats equal-capital net IR at both floors (e.g. $2B: 0.60 vs 0.52) but paired IC t ≈0–0.2 (not significant); max-Sharpe allocation fails/inconsistent across floors (−0.15 to +0.65)                                      | Not adopted (ERC — "suggestive only"); killed (max-Sharpe)                  | Maybe — ERC is a defensible practical framing even without a statistically confirmed IC gain | Return-based allocation chases stale sleeve Sharpes and misses regime shifts, echoing `factor_filter`                                                                             |

## 5. Target / label engineering & training methodology

Changing what the model is trained to predict, on what data, or with what loss — not new inputs.

| Experiment                                                            | Objective                                                                                                                                               | Key result                                                                                                                                                                                                                                  | Verdict                                                   | Pursue further?                                                      | Notes                                                                                                                                                                                   |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`composite_prior`](../experiments/composite_prior/README.md)         | Can ML anchored to the composite (LGBM init_score, ET-on-residual, ridge-to-prior, orthogonal residual) beat the composite outright                     | Every ML arm below the composite: paired IC diff −0.011 to −0.018; orthogonal-residual-alone IC 0.0005 (below the 0.01 kill bar); gross IR −0.29 to +0.11 vs. composite's 0.615                                                             | Killed — both pre-registered kill rules fired             | No — the 18 factors hold no learnable structure beyond the composite | Found/fixed an unfloored-slope bug (sign flip in a 2023 fold); conclusion unchanged after the fix                                                                                       |
| [`era_split`](../experiments/era_split/README.md)                     | Era-Splitting criteria (a split must help in every era), plus a size-environment extension, to beat the pooled-criterion tree / composite               | All raw era criteria worse than the pooled control (IC 0.0181 vs e.g. `era_avg` 0.0054); best composite-init variant still below the composite (0.0212 vs 0.0366)                                                                           | Killed — pre-registered rule fires for all arms           | No — explicitly "not warranted"                                      | Same unfloored-slope bug found/fixed as `composite_prior`; a dependent two-stage extension also killed since its precondition failed                                                    |
| [`recency_window`](../experiments/recency_window/README.md)           | Down-weight/drop old training months (half-life or rolling window) or era-stack ET/LightGBM models vs. an expanding-window control                      | All recency arms below the control (ET 0.014–0.015 vs 0.016; LGBM 0.008–0.013 vs 0.016); best arm (era-stacking) IC 0.0196, paired t 1.4–1.5 — below the rule                                                                               | Killed                                                    | No                                                                   | Recency hurts, if anything; only era-stacking is non-negative but not significant                                                                                                       |
| [`target_decile_class`](../experiments/target_decile_class/README.md) | Predict within-month decile (10-class, prob-weighted expected decile) vs. regressing raw return (Bai–Pukthuanthong)                                     | Best arm IC 0.024 vs control 0.016 (paired t 1.04); portfolios worse than the control (gross IR −0.32/−0.35 vs −0.17/−0.22); argmax/top-minus-bottom worse still                                                                            | Killed                                                    | No                                                                   | 1 of 4 target-transform experiments sharing the frozen `largecap` harness; only positive-sign variant, not independent evidence                                                         |
| [`target_neutral`](../experiments/target_neutral/README.md)           | Demean the training target by size tercile, GICS sector, both, or factor-residual, to raise tradeable IC                                                | Sector-demean / factor-residual: +0.002–0.005 IC (not significant); size-demean hurts (IC 0.012 vs 0.016 control); no arm meets the rule                                                                                                    | Killed                                                    | No                                                                   | Size-median removal (a literature finding) does not transfer to the $2B+ universe; the factor-residual idea was retested combined with the composite in `composite_prior` (also failed) |
| [`target_rank`](../experiments/target_rank/README.md)                 | Train on within-month uniform/Gaussian rank instead of the winsorized raw return (Cakici & Zaremba)                                                     | +0.003–0.006 IC in both model families and 3 seeds (consistent direction) but paired t only 0.7–1.4, below the ≥2 rule; all `lc_t10` books still negative                                                                                   | Killed (direction of effect noted)                        | Maybe — keep as a cheap add-on atop another signal, not standalone   | Combined test in `composite_prior` also failed                                                                                                                                          |
| [`target_ranker`](../experiments/target_ranker/README.md)             | Learning-to-rank objectives (XGBRanker ndcg/pairwise, LGBMRanker lambdarank, a custom rank-IC-weighted pairwise loss) vs. squared-error regression      | Best: XGB rank_pairwise IC 0.0152 vs its own (unusually weak) control 0.0055 (paired t 1.57); LGBM lambdarank IC ≈ its control (t 0.03); all portfolios negative (gross IR −0.32 to −0.68)                                                  | Killed                                                    | No                                                                   | Last of 4 target experiments; the XGB control is unusually weak, inflating the apparent ranker gain; the custom loss is no better than off-the-shelf pairwise                           |
| [`train_weighting`](../experiments/train_weighting/README.md)         | Weight training loss toward liquid/large names (√mcap, mcap, √dolvol), or train on all stocks with such weights, vs. a universe-only unweighted control | All weighted arms hurt (IC 0.006–0.013 vs 0.016); dollar-volume weighting significantly worse (paired t −2.7/−2.9); best fitted arm (LGBM, all-stock, unweighted) IC 0.023 (t 1.83) but not a significant gain over the control (t 0.9–1.1) | Killed (weighting); not adopted (all-stock, inconclusive) | Maybe — all-stock/unweighted is a low-priority follow-up             | Weighting toward liquid names removes information the model needs (an effective-sample-size loss); composite IC 0.037 still well above the best fitted arm here                         |

## 6. Desk system: Factors desk + Text desk + Deterministic PM (`fiam_desks/`, see `docs/DESKS.md`)

Built on the DEV window 2015-02..2020-12 (decisions) and confirmed once on 2021-01..2026-08 (`desk_09`, arms and rules pre-registered; every test look in `experiments/desk_test_ledger.csv`).

| Experiment | Objective | Key result | Verdict | Pursue further? |
| --- | --- | --- | --- | --- |
| [`desk_00_infra_audit`](../experiments/desk_00_infra_audit/README.md) | Reproduce the frozen composite/LP; audit the teammate's text tables and the desk pipeline for look-ahead | Reproduces IC 0.0366 and net IR 0.541 exactly; txt_v1 = raw 8-K counts (0 mismatches); novelty past-only match 0.95 vs future-inclusive 0.85; truncation and determinism pass | Adopted (regression gate) | Re-run after any change |
| [`desk_01_text_signals_dev`](../experiments/desk_01_text_signals_dev/README.md) | Do txt_v1/v2 signals add to the composite on the tradeable universe (dev)? | All 18 tests killed; handoff IC replicates on ALL stocks given a filing (-0.012, t -2.5) but is ~0 at >= $2B | Killed | No |
| [`desk_02_text_by_size_band`](../experiments/desk_02_text_by_size_band/README.md) | Where does the text edge live; does a lower universe floor help? | Edge only in screen-failing names; opposite sign at $0.5-2B; no floor rescues it | Killed | No |
| [`desk_03_text_roles_dev`](../experiments/desk_03_text_roles_dev/README.md) | Other roles: persistence, news-gated reversal, tail overlay | 5.02 decayed intensity has the OPPOSITE sign on 2015-20 (t -2.4); gating worse than plain reversal; one tail flag borderline | Killed (text) | No |
| [`desk_04_text_rare_event_veto`](../experiments/desk_04_text_rare_event_veto/README.md) | Do 4.01/4.02/2.05/2.06 events select tail losers among long candidates? | No: flagged names have thinner, not fatter, left tails | Killed | No |
| [`desk_05_factors_desk_dev`](../experiments/desk_05_factors_desk_dev/README.md) | Factors-desk candidates (reversal, sector-neutral) and the composite's dev baseline | Composite book LOSES on dev (net IR -0.87, 2020 -29.9%); rev_1m inconclusive, others killed | Diagnostic + 2 killed | Regime risk: yes |
| [`desk_06_pm_neutral_dial_dev`](../experiments/desk_06_pm_neutral_dial_dev/README.md) | Is the dev loss the LP's or the signal's? (neutral_dial on dev) | Deeper neutralisation is worse (D4 net IR -1.66): the signal's regime, not the PM | Not adopted | No |
| [`desk_07_factors_momentum_dev`](../experiments/desk_07_factors_momentum_dev/README.md) | Does a momentum group repair the factors desk on dev? | IC -0.005 (t -0.27), gain -0.0036 (t -0.59) | Killed | No |
| [`desk_08_pm_integration_dev`](../experiments/desk_08_pm_integration_dev/README.md) | PM modes with the text desk (blend, veto, tilt, dial, judge) + shuffle placebos | Constraints hold; all paired net t < 1 in absolute value; only tilt/judge weakly supportive | Not adopted (null) | No |
| [`desk_09_confirmation_test`](../experiments/desk_09_confirmation_test/README.md) | One pre-registered test-window confirmation of bases, text modes, rev_1m, standalone text | Bases reproduce (0.637 net for F1+cap); NO text mode adopted (tilt IC paired t -2.5); novneg_max replicated standalone (IC 0.0103, t 2.87) | Confirmed / text not adopted | No |
| [`desk_10_final_system`](../experiments/desk_10_final_system/README.md) | Assemble and audit the default system, write FIAM-format outputs | Net IR 0.637, beta +0.02, max DD -9.8%, 203-248 positions, constraints pass every month; same system on dev -0.76 | Adopted | Generate MAIN.py |
| [`desk_11_devils_advocate`](../experiments/desk_11_devils_advocate/README.md) | Deterministic attacks on the default system | Alpha t 1.07 after style proxies (R2 0.75); IR 0.98 -> 0.21 between halves; text modes indistinguishable (bootstrap CIs include 0) | Diagnostic (2 of 4 criteria fail) | Yes: disclose |
| [`desk_12_novneg_posthoc`](../experiments/desk_12_novneg_posthoc/README.md) | POST-HOC: novelty x negative as an 8th group, both periods | Same-sign alone (pooled IC +0.0087, t 3.06) but dilutes the test base (paired IC t -2.09) | Post-hoc, not adopted | Only with new data |

## 7. Research run 2026-09-24 (`fiam_research/`, see `docs/RESEARCH_RUN_2026-09-24.md`)

Decisions on DEV; one pre-registered TEST confirmation (desk_27). Every variant is in `experiments/desk_research_ledger.csv`.

| Experiment | Objective | Key result | Verdict | Pursue further? |
| --- | --- | --- | --- | --- |
| [`desk_13_baseline_freeze`](../experiments/desk_13_baseline_freeze/README.md) | Freeze B0 with the full metric suite | DEV IR −0.757 / TEST +0.637; holdings quality +0.95, low-vol +0.35; DEV beta −0.17 | Reference | – |
| [`desk_14_si_history_extension`](../experiments/desk_14_si_history_extension/README.md) | FINRA SI 2018-01..2020-05 (new data); fresh test of dtc and SI cap | dtc IC −0.001 (not replicated); SI cap paired t +2.12 | Data adopted; cap confirmed; dtc unconfirmed | – |
| [`desk_15_data_audit`](../experiments/desk_15_data_audit/README.md) | Data / implementation audit | Delisting rows dropped before universe formation (look-ahead); fix moves DEV IR −0.10 | **Correction adopted (B1)** | – |
| [`desk_16_literature`](../experiments/desk_16_literature/README.md) | Literature review + plan | 18 sources → hypotheses | – | – |
| [`desk_17_regime_attribution`](../experiments/desk_17_regime_attribution/README.md) | Why DEV loses | Value crash + low-vol / investment losses; dispersion predicts IC (t −5.4 / −3.1) | Diagnostic | – |
| [`desk_18_noise_floor`](../experiments/desk_18_noise_floor/README.md) | LP path noise | Jitter sd 0.03 IR; random −1.36 | Diagnostic | – |
| [`desk_19_signal_construction`](../experiments/desk_19_signal_construction/README.md) | JKP 13 literature-signed themes | Steadier book, lower IC; no DEV evidence | No evidence | Carried → A5 |
| [`desk_20_portfolio_construction`](../experiments/desk_20_portfolio_construction/README.md) | Vol caps, 0.5% cap, risk QP, tracking, SI-cap level | LP not the bottleneck; SI cap 5% t +3.19 | QP rejected; cap 5% carried | No |
| [`desk_21_regime_timing`](../experiments/desk_21_regime_timing/README.md) | Factor momentum, inverse-vol, dispersion scaling | Nothing passes | No evidence | No |
| [`desk_22_signal_decay`](../experiments/desk_22_signal_decay/README.md) | Horizons 1–12m, turnover budget | Profitability / quality slow; budget irrelevant | Diagnostic | No |
| [`desk_23_text_reopened`](../experiments/desk_23_text_reopened/README.md) | Text roles | No return signal; **text predicts idio risk (t 4.65)** | Risk info only | Maybe (risk model) |
| [`desk_24_interactions`](../experiments/desk_24_interactions/README.md) | Conditional ICs, multiplicative signals | None; high-SI IC −0.05 on DEV | Rejected | No |
| [`desk_25_model_classes`](../experiments/desk_25_model_classes/README.md) | Ridge / monotone GBM walk-forward | GBM IC +0.023 OOS 2017–20 | No evidence | Carried → A3 |
| [`desk_26_multiple_testing`](../experiments/desk_26_multiple_testing/README.md) | PBO / DSR / FDR | PBO 0.49; 1 BH survivor | Diagnostic | – |
| [`desk_27_confirmation_test`](../experiments/desk_27_confirmation_test/README.md) | One pre-registered TEST look | No arm passes; cap 5% reverses (t −2.0); GBM IC 0.017; JKP13z Sharpe 1.29 vs 0.99 | **B1 stays** | – |
| [`desk_28_posthoc_diagnostics`](../experiments/desk_28_posthoc_diagnostics/README.md) | Why (post-hoc) | Flips = growth-vs-value cycle; JKP13z Sharpe diff +0.29 [−0.14, 0.73] | Descriptive | – |
| [`desk_29_final_candidate`](../experiments/desk_29_final_candidate/README.md) | FIAM outputs B1 + A5 | Constraints / determinism / truncation pass | Output | Forward-test A5 |

---

### Legend

- **Killed** — failed the experiment's own pre-registered kill rule; not used.
- **Not adopted** — run to completion, result present but statistically inconclusive (t roughly 1–2, or a real-but-too-small effect); not used as-is.
- **Adopted** — met its bar and is (or should be) in active use / the current reference.
- **Diagnostic only** — no adopt/reject rule was ever defined; the experiment's value is the standing fact or methodology it established, not a signal to trade.
- **Blocked** — could not be run to its intended conclusion for lack of data (e.g. I/B/E/S analyst revisions, daily returns).
